name: CI

on:
  push:
    branches:
      - main

jobs:
  ci:
    runs-on: ubuntu-latest

    env:
      NX_BASE: ${{ github.event.before }}
      NX_HEAD: ${{ github.sha }}

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - run: npm ci

      # TODO: revert to `nx affected` after first successful run
      - run: npx nx run-many -t lint --parallel=3
      - run: npx nx run-many -t test --parallel=3
      - run: npx nx run-many -t build --parallel=3
