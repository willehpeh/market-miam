# admin-frontend

Internal admin Angular app. Early stage — currently the default scaffold; will
become the internal console for platform operations.

```sh
npx nx serve admin-frontend
npx nx test admin-frontend
npx nx build admin-frontend
```
