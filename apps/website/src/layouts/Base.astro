---
// The two typefaces, self-hosted — see packages/design-system/fonts.css. It has
// to be a frontmatter import: url() inside an @import'ed sheet is not rebased,
// so the woff2 would be looked for next to the page instead of the bundle.
import '../../../../packages/design-system/fonts.css';

// The bundler hashes the emitted file, so the preload below has to ask it for
// the URL rather than name one. Same file the first @font-face picks.
import hankenLatin from '../../../../packages/design-system/fonts/hanken-grotesk-latin.woff2?url';

interface Props {
  title: string;
  description?: string;
}

const {
  title,
  description = 'Traiteurs de marché : mieux servir vos habitués, mieux préparer chaque marché. Votre vitrine, votre carte, vos marchés — et le menu du jour, chaque matin.',
} = Astro.props;

// Astro.site comes from `site` in astro.config.mjs — the one place the origin lives.
const canonical = new URL(Astro.url.pathname, Astro.site);
const ogImage = new URL('/og.jpg', Astro.site);

const schema = {
  '@context': 'https://schema.org',
  '@graph': [
    {
      '@type': 'Organization',
      '@id': new URL('/#organization', Astro.site).href,
      name: 'Market Miam',
      url: Astro.site?.href,
      logo: new URL('/logo.webp', Astro.site).href,
      email: 'contact@marketmiam.fr',
    },
    {
      '@type': 'WebSite',
      '@id': new URL('/#website', Astro.site).href,
      name: 'Market Miam',
      url: Astro.site?.href,
      inLanguage: 'fr',
      publisher: { '@id': new URL('/#organization', Astro.site).href },
    },
  ],
};
---

<!doctype html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />

    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="Market Miam" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:locale" content="fr_FR" />
    <meta property="og:image" content={ogImage} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:alt" content="Market Miam — l’adresse de votre stand" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:image" content={ogImage} />

    <link
      rel="preload"
      href={hankenLatin}
      as="font"
      type="font/woff2"
      crossorigin
    />
    <title>{title}</title>
    <script is:inline type="application/ld+json" set:html={JSON.stringify(schema)} />
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  @import "../../../../packages/design-system/tokens.css";

  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  html {
    scroll-behavior: smooth;
  }

  body {
    font-family: var(--mm-font-sans);
    background: var(--mm-canvas);
    color: var(--mm-ink);
    line-height: 1.55;
    -webkit-font-smoothing: antialiased;
  }

  :focus-visible {
    outline: 2px solid var(--mm-brand-deep);
    outline-offset: 3px;
  }

  @media (prefers-reduced-motion: reduce) {
    html { scroll-behavior: auto; }
  }
</style>
