# admin-api

Internal admin backend (NestJS). Early stage — currently the default scaffold;
will grow into the operational/support surface for the platform.

```sh
npx nx serve admin-api
npx nx build admin-api
```
